kotlin.code.style=official

# Faster & cache-friendly builds
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Consumed by the publishing-conventions plugin.
library.group=ru.swdevsm
library.version=0.0.1

# Override detekt engine version without editing the convention plugin.
detekt.version=1.23.8